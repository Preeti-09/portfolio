import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    title:{
        marginBottom :"20px",
    },
    card:{
        background:"linear-gradient(#1000ff,rgb(2, 0, 34))",
        color:"#fff",
    }
}))